[project]
name = "lifeofpy-checksum"
version = "0.1.0"
description = "Checksum generation for LifeOfPy"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/checksum_generator"]
