name: Release

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Install uv
      uses: astral-sh/setup-uv@v2
      
    - name: Set up Python
      run: uv python install 3.12
      
    - name: Build and Publish Placeholder
      run: |
        echo "Placeholder for future PyPI publishing steps"
        echo "e.g. uv build && uv publish"
